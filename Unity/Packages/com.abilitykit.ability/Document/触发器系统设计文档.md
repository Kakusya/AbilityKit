# Ability-Kit 触发器系统设计文档

> **阅读对象**：希望了解触发器系统设计的开发者
>
> **文档目标**：让你理解"通用事件触发器与强类型触发器的区别"、"两种实现各自的设计理念"、"如何选择合适的触发器方案"

---

## 一、设计理念：两种触发器范式的对比

### 1.1 通用事件触发器 vs 强类型触发器

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        两种触发器范式对比                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  【通用事件触发器】（Ability.Triggering 包）                            │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  核心思想：事件 = 字符串ID + 泛型上下文                           │  │
│  │                                                                  │  │
│  │  EventId = "skill.cast.start"  // 字符串标识                    │  │
│  │  Context = TriggerContext              // 通用上下文              │  │
│  │                                                                  │  │
│  │  特点：                                                          │  │
│  │  - 完全解耦，事件订阅方不需要知道发布方                           │  │
│  │  - 支持运行时动态配置事件和动作                                  │  │
│  │  - 可以从 JSON/Excel 驱动触发计划                              │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  【强类型触发器】（MobaBuffTriggering 等）                           │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  核心思想：事件 = 类型安全的结构体                               │  │
│  │                                                                  │  │
│  │  Events.CastStart = "skill.cast.start"    // 字符串常量         │  │
│  │  Context = SkillCastContext              // 强类型上下文         │  │
│  │                                                                  │  │
│  │  特点：                                                          │  │
│  │  - 编译时类型检查，IDE 自动补全                                  │  │
│  │  - 性能更好，不需要反射                                          │  │
│  │  - 代码即文档，易于维护                                          │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心问题：何时需要通用触发器？

```
❌ 不是所有场景都需要通用触发器

┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  【简单场景：直接调用】                                                │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  // 直接调用，不需要事件系统                                     │  │
│  │  if (target.Health <= 0)                                        │  │
│  │  {                                                              │  │
│  │      OnUnitDeath(target);  // 直接方法调用                      │  │
│  │  }                                                              │  │
│  │                                                                  │  │
│  │  什么时候需要事件：                                              │  │
│  │  - 需要解耦多个系统                                              │  │
│  │  - 需要运行时配置响应逻辑                                        │  │
│  │  - 需要支持 Mod/热更新                                          │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ✅ 适合使用通用触发器的场景                                          │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  1. 复杂效果链                                                  │  │
│  │     - "当受到伤害时，有30%几率触发反击"                         │  │
│  │     - 条件（受伤） + 概率（30%） + 动作（反击）                │  │
│  │                                                                  │  │
│  │  2. 多系统联动                                                  │  │
│  │     - 技能释放 → 触发 Buff → 更新 UI → 播放音效 → 记录统计  │  │
│  │                                                                  │  │
│  │  3. 配置驱动的玩法                                              │  │
│  │     - 从 Excel 配置触发计划，运行时解析执行                    │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、通用事件触发器设计

### 2.1 核心架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    通用事件触发器架构                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                         IEventBus                               │  │
│  │                      （事件总线）                               │  │
│  │  ┌───────────────────────────────────────────────────────────┐ │  │
│  │  │  Subscribe(eventId, handler)                            │ │  │
│  │  │  Publish(event)                                        │ │  │
│  │  └───────────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────┬────────────────────────────────────┘  │
│                               │                                         │
│                               ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                    TriggerRegistry                               │  │
│  │                 （触发器注册表）                                 │  │
│  │  ┌───────────────────────────────────────────────────────────┐ │  │
│  │  │  _conditionFactories: Dictionary<string, IConditionFactory>│
│  │  │  _actionFactories: Dictionary<string, IActionFactory>      │ │
│  │  │                                                            │ │  │
│  │  │  AutoRegisterFromAssemblies()                             │ │  │
│  │  │  CreateCondition(def)                                     │ │  │
│  │  │  CreateAction(def)                                         │ │  │
│  │  └───────────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────┬────────────────────────────────────┘  │
│                               │                                         │
│                               ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                  TriggerActionRunner                             │  │
│  │                  （动作运行器）                                 │  │
│  │  ┌───────────────────────────────────────────────────────────┐ │  │
│  │  │  _running: List<Entry>                                 │ │  │
│  │  │                                                            │ │  │
│  │  │  Add(action, ownerKey)                                   │ │  │
│  │  │  Tick(deltaTime)                                          │ │  │
│  │  │  CancelByOwnerKey(ownerKey)                              │ │  │
│  │  └───────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心接口

```csharp
namespace AbilityKit.Ability.Triggering
{
    /// <summary>
    /// 事件总线接口
    /// </summary>
    public interface IEventBus
    {
        /// <summary>
        /// 订阅事件
        /// </summary>
        IEventSubscription Subscribe(string eventId, IEventHandler handler);

        /// <summary>
        /// 发布事件
        /// </summary>
        void Publish(in TriggerEvent evt);
    }
}

namespace AbilityKit.Ability.Triggering.Runtime
{
    /// <summary>
    /// 触发动作运行器接口
    /// </summary>
    public interface ITriggerActionRunner
    {
        int RunningCount { get; }

        /// <summary>
        /// 添加运行中的动作
        /// </summary>
        void Add(IRunningAction action, object owner = null);
        void Add(IRunningAction action, long ownerKey);

        /// <summary>
        /// 每帧 Tick
        /// </summary>
        void Tick(float deltaTime);

        /// <summary>
        /// 取消动作
        /// </summary>
        int CancelByOwner(object owner);
        int CancelByOwnerKey(long ownerKey);
        int CancelAll();
    }
}
```

### 2.3 触发器注册

```csharp
namespace AbilityKit.Ability.Triggering.Runtime
{
    /// <summary>
    /// 触发器注册表
    /// 支持从程序集自动注册条件和动作工厂
    /// </summary>
    public sealed class TriggerRegistry
    {
        private readonly Dictionary<string, IConditionFactory> _conditionFactories;
        private readonly Dictionary<string, IActionFactory> _actionFactories;

        /// <summary>
        /// 从程序集自动注册
        /// </summary>
        public void AutoRegisterFromAssemblies(params Assembly[] assemblies)
        {
            foreach (var type in assemblies.GetTypes())
            {
                // 查找 [TriggerActionType] 属性
                var actionAttr = type.GetCustomAttribute<TriggerActionTypeAttribute>();
                if (actionAttr != null && typeof(IActionFactory).IsAssignableFrom(type))
                {
                    var factory = CreateFactoryInstance<IActionFactory>(type);
                    RegisterAction(actionAttr.Type, factory);
                }

                // 查找 [TriggerConditionType] 属性
                var conditionAttr = type.GetCustomAttribute<TriggerConditionTypeAttribute>();
                if (conditionAttr != null && typeof(IConditionFactory).IsAssignableFrom(type))
                {
                    var factory = CreateFactoryInstance<IConditionFactory>(type);
                    RegisterCondition(conditionAttr.Type, factory);
                }
            }
        }

        /// <summary>
        /// 创建条件
        /// </summary>
        public ITriggerCondition CreateCondition(ConditionDef def)
        {
            if (!_conditionFactories.TryGetValue(def.Type, out var factory))
                throw new InvalidOperationException($"Condition type not registered: {def.Type}");
            return factory.Create(def);
        }

        /// <summary>
        /// 创建动作
        /// </summary>
        public ITriggerAction CreateAction(ActionDef def)
        {
            if (!_actionFactories.TryGetValue(def.Type, out var factory))
                throw new InvalidOperationException($"Action type not registered: {def.Type}");
            return factory.Create(def);
        }
    }
}
```

---

## 三、动作运行器设计

### 3.1 动作接口

```csharp
namespace AbilityKit.Ability.Triggering.Runtime
{
    /// <summary>
    /// 运行中的动作接口
    /// </summary>
    public interface IRunningAction : IDisposable
    {
        bool IsDone { get; }
        void Tick(float deltaTime);
        void Cancel();
    }
}
```

### 3.2 运行器实现

```csharp
namespace AbilityKit.Ability.Triggering.Runtime
{
    public sealed class TriggerActionRunner : ITriggerActionRunner
    {
        private struct Entry
        {
            public IRunningAction Action;
            public object Owner;
            public long OwnerKey;
        }

        private readonly List<Entry> _running = new List<Entry>(64);

        public void Add(IRunningAction action, long ownerKey)
        {
            _running.Add(new Entry { Action = action, OwnerKey = ownerKey });
        }

        public void Tick(float deltaTime)
        {
            for (int i = _running.Count - 1; i >= 0; i--)
            {
                var e = _running[i];
                var a = e.Action;

                if (a.IsDone)
                {
                    TryDispose(a);
                    _running.RemoveAt(i);
                    continue;
                }

                a.Tick(deltaTime);
            }
        }

        /// <summary>
        /// 按 OwnerKey 取消动作
        /// 用于 EffectSource 结束时取消所有关联动作
        /// </summary>
        public int CancelByOwnerKey(long ownerKey)
        {
            var count = 0;
            for (int i = _running.Count - 1; i >= 0; i--)
            {
                if (_running[i].OwnerKey != ownerKey) continue;

                _running[i].Action?.Cancel();
                TryDispose(_running[i].Action);
                _running.RemoveAt(i);
                count++;
            }
            return count;
        }
    }
}
```

---

## 四、条件和动作系统

### 4.1 条件定义

```csharp
namespace AbilityKit.Ability.Triggering.Runtime
{
    /// <summary>
    /// 触发条件接口
    /// </summary>
    public interface ITriggerCondition
    {
        bool Evaluate(TriggerContext context);
    }
}
```

### 4.2 条件实现示例

```csharp
namespace AbilityKit.Ability.Triggering.Runtime.Builtins
{
    /// <summary>
    /// Any 条件：任意子条件满足即通过
    /// </summary>
    [TriggerConditionType("any")]
    public sealed class AnyCondition : ITriggerCondition
    {
        private readonly List<ITriggerCondition> _children;

        public AnyCondition(IReadOnlyList<ITriggerCondition> children)
        {
            _children = new List<ITriggerCondition>(children);
        }

        public bool Evaluate(TriggerContext context)
        {
            foreach (var child in _children)
            {
                if (child.Evaluate(context)) return true;
            }
            return false;
        }

        public static AnyCondition FromDef(ConditionDef def, IConditionCompiler compiler)
        {
            var items = (List<ConditionDef>)def.Args["items"];
            var compiled = items.Select(c => compiler.Compile(c)).ToList();
            return new AnyCondition(compiled);
        }
    }

    /// <summary>
    /// 数值变量大于条件
    /// </summary>
    [TriggerConditionType("numeric.gt")]
    public sealed class NumericVarGreaterThanCondition : ITriggerCondition
    {
        private readonly NumericVarRef _var;
        private readonly double _threshold;

        public bool Evaluate(TriggerContext context)
        {
            if (context.TryGetNumericVar(_var, out var value))
            {
                return value > _threshold;
            }
            return false;
        }
    }
}
```

### 4.3 动作定义

```csharp
namespace AbilityKit.Ability.Triggering.Runtime
{
    /// <summary>
    /// 触发动作接口
    /// </summary>
    public interface ITriggerAction
    {
        void Execute(TriggerContext context);
    }
}
```

### 4.4 动作实现示例

```csharp
namespace AbilityKit.Ability.Triggering.Runtime.Builtins
{
    /// <summary>
    /// 设置数值变量动作
    /// </summary>
    [TriggerActionType("setNumericVar")]
    public sealed class SetNumericVarAction : ITriggerAction
    {
        private readonly NumericVarRef _var;
        private readonly NumericValueSourceRuntime _value;

        public SetNumericVarAction(NumericVarRef varRef, NumericValueSourceRuntime value)
        {
            _var = varRef;
            _value = value;
        }

        public void Execute(TriggerContext context)
        {
            if (NumericValueSourceRuntimeUtil.TryResolve(context, _value, out var v))
            {
                context.TrySetNumericVar(_var, v);
            }
        }
    }
}
```

---

## 五、数值变量系统

### 5.1 变量引用

```csharp
namespace AbilityKit.Ability.Triggering.Variables.Numeric
{
    /// <summary>
    /// 数值变量引用
    /// </summary>
    public readonly struct NumericVarRef : IEquatable<NumericVarRef>
    {
        public readonly string DomainId;  // 域 ID
        public readonly string Key;       // 变量键

        public NumericVarRef(string domainId, string key)
        {
            DomainId = domainId;
            Key = key;
        }
    }
}
```

### 5.2 变量域

```csharp
namespace AbilityKit.Ability.Triggering.Variables.Numeric.Domains
{
    /// <summary>
    /// 局部变量域（绑定到 TriggerContext）
    /// </summary>
    public sealed class LocalNumericVarDomain : INumericVarDomain
    {
        private readonly Dictionary<string, double> _vars = new();

        public bool TryGet(TriggerContext context, string key, out double value)
        {
            return _vars.TryGetValue(key, out value);
        }

        public bool TrySet(TriggerContext context, string key, double value)
        {
            _vars[key] = value;
            return true;
        }
    }

    /// <summary>
    /// 全局变量域
    /// </summary>
    public sealed class GlobalNumericVarDomain : INumericVarDomain
    {
        private readonly Dictionary<string, double> _vars = new();
        private readonly object _lock = new();

        public bool TryGet(TriggerContext context, string key, out double value)
        {
            lock (_lock)
            {
                return _vars.TryGetValue(key, out value);
            }
        }

        public bool TrySet(TriggerContext context, string key, double value)
        {
            lock (_lock)
            {
                _vars[key] = value;
                return true;
            }
        }
    }
}
```

### 5.3 变量扩展方法

```csharp
namespace AbilityKit.Ability.Triggering.Variables.Numeric
{
    public static class TriggerContextNumericVarExtensions
    {
        public static bool TryGetNumericVar(
            this TriggerContext context,
            in NumericVarRef varRef,
            out double value)
        {
            var registry = context.Services.GetService<INumericVarDomainRegistry>();
            if (registry.TryGetDomain(varRef.DomainId, out var domain))
            {
                return domain.TryGet(context, varRef.Key, out value);
            }
            value = 0d;
            return false;
        }

        public static bool TrySetNumericVar(
            this TriggerContext context,
            in NumericVarRef varRef,
            double value)
        {
            var registry = context.Services.GetService<INumericVarDomainRegistry>();
            if (registry.TryGetDomain(varRef.DomainId, out var domain))
            {
                return domain.TrySet(context, varRef.Key, value);
            }
            return false;
        }
    }
}
```

---

## 六、强类型触发器设计

### 6.1 强类型事件定义

```csharp
namespace AbilityKit.Ability.Share.Impl.Moba.Services
{
    /// <summary>
    /// MOBA Buff 触发器
    /// 强类型实现
    /// </summary>
    public static class MobaBuffTriggering
    {
        /// <summary>
        /// 事件 ID 常量
        /// </summary>
        public static class Events
        {
            public const string ApplyOrRefresh = "buff.apply";
            public const string Remove = "buff.remove";
            public const string Interval = "buff.interval";

            /// <summary>
            /// 生成带效果 ID 的事件名
            /// </summary>
            public static string WithEffect(string baseEventId, int effectId)
            {
                return $"{baseEventId}.{effectId}";
            }
        }

        /// <summary>
        /// 事件参数字段名
        /// </summary>
        public static class Args
        {
            public const string BuffId = "buff.id";
            public const string EffectId = "buff.effectId";
            public const string StackCount = "buff.stackCount";
            public const string DurationSeconds = "buff.durationSeconds";
            public const string Stage = "buff.stage";
            public const string RemoveReason = "buff.removeReason";
        }
    }

    /// <summary>
    /// MOBA 技能触发器
    /// </summary>
    public static class MobaSkillTriggering
    {
        public static class Events
        {
            public const string PreCastStart = "skill.precast.start";
            public const string PreCastComplete = "skill.precast.complete";
            public const string PreCastFail = "skill.precast.fail";
            public const string PreCastInterrupt = "skill.precast.interrupt";

            public const string CastStart = "skill.cast.start";
            public const string CastComplete = "skill.cast.complete";
            public const string CastFail = "skill.cast.fail";
            public const string CastInterrupt = "skill.cast.interrupt";
        }

        /// <summary>
        /// 发布技能触发事件
        /// </summary>
        public static void Publish(string eventId, SkillCastContext ctx, string failReason = null)
        {
            if (string.IsNullOrEmpty(eventId)) return;
            if (ctx == null) return;

            // 获取事件总线
            var services = ctx.WorldServices;
            if (!services.TryResolve<IEventBus>(out var planBus)) return;

            // 发布事件
            var eid = TriggeringIdUtil.GetEventEid(eventId);
            planBus.Publish(new EventKey<SkillCastContext>(eid), ctx);
            planBus.Publish(new EventKey<object>(eid), ctx);
        }
    }
}
```

---

## 七、两种实现对比

### 7.1 特性对比

| 特性 | 通用触发器 | 强类型触发器 |
|------|-----------|-------------|
| **类型安全** | ❌ 运行时检查 | ✅ 编译时检查 |
| **IDE 支持** | ❌ 无自动补全 | ✅ 完整自动补全 |
| **性能** | ⚠️ 需要反射/字典查找 | ✅ 直接调用 |
| **灵活性** | ✅ 支持运行时配置 | ❌ 硬编码 |
| **可读性** | ⚠️ 字符串ID需查文档 | ✅ 代码即文档 |
| **热更新** | ✅ 可从 JSON 驱动 | ❌ 需要代码修改 |
| **学习成本** | ⚠️ 较高 | ✅ 较低 |

### 7.2 适用场景对比

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        场景选择指南                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  【选择通用触发器的场景】                                              │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  1. 复杂效果链                                                  │  │
│  │     "当 A 发生时，如果 B 且（ C 或 D ），则执行 X、Y、Z"       │  │
│  │     - 需要组合条件                                              │  │
│  │     - 需要运行时配置                                            │  │
│  │                                                                  │  │
│  │  2. 配置驱动的玩法                                              │  │
│  │     - 从 Excel 读取触发计划                                     │  │
│  │     - 支持 Mod/热更新                                            │  │
│  │                                                                  │  │
│  │  3. 需要解耦多个系统                                            │  │
│  │     - 技能释放 → Buff → UI → 音效 → 统计                      │  │
│  │     - 发布方不关心订阅方                                        │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  【选择强类型触发器的场景】                                            │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  1. 简单的事件订阅                                              │  │
│  │     "当技能释放时，更新冷却"                                    │  │
│  │     - 事件类型少                                                │  │
│  │     - 响应逻辑固定                                              │  │
│  │                                                                  │  │
│  │  2. 性能敏感的场景                                              │  │
│  │     - 高频事件（如移动、碰撞）                                  │  │
│  │     - 需要最小化开销                                            │  │
│  │                                                                  │  │
│  │  3. 团队成员不熟悉通用触发器                                    │  │
│  │     - 学习成本高                                                │  │
│  │     - 强类型更直观                                              │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 八、混合使用策略

### 8.1 推荐架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      推荐：分层触发器架构                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  【表现层】强类型触发器                                                │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  // 直接订阅，强类型                                            │  │
│  │  _eventBus.Subscribe(MobaSkillTriggering.Events.CastStart,     │  │
│  │      (ctx) => UpdateUI(ctx));                                   │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│                              │                                         │
│                              ▼                                         │
│  【逻辑层】通用触发器（可选）                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  // 从配置读取的复杂效果链                                       │  │
│  │  TriggerPlan plan = LoadFromConfig("skill_passive_1.json");    │  │
│  │  plan.Execute(context);                                         │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│                              │                                         │
│                              ▼                                         │
│  【数据层】直接方法调用                                                │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  // 对于简单场景，直接方法调用                                    │  │
│  │  if (target.IsDead)                                             │  │
│  │  {                                                              │  │
│  │      UnitDeathSystem.Handle(target);                            │  │
│  │  }                                                              │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.2 架构原则

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        架构分层原则                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  【Rule 1：优先直接调用】                                              │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  不要为了"解耦"而引入事件系统                                   │  │
│  │  只有当直接调用导致问题时才使用事件                            │  │
│  │                                                                  │  │
│  │  ❌ 过度设计：                                                  │  │
│  │     OnDamage() → Publish("unit.damaged", ctx) → OnUnitDamaged()│  │
│  │                                                                  │  │
│  │  ✅ 简单直接：                                                  │  │
│  │     OnDamage(target, damage) → target.Health -= damage;        │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  【Rule 2：强类型优先】                                                │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  优先使用强类型触发器                                          │  │
│  │  只有在需要配置驱动时才使用通用触发器                         │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  【Rule 3：按需引入】                                                  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │  功能复杂度增加路径：                                          │  │
│  │  直接调用 → 强类型事件 → 通用触发器                        │  │
│  │                                                                  │  │
│  │  只有当低层方案无法满足需求时才升级到更高层                   │  │
│  │                                                                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 九、Trigger Authoring 编辑器治理

### 9.1 当前所有权

Trigger authoring 的 Unity 资产、模板、project/catalog、validator、Source JSON codec、source sync baseline 和 runtime plan exporter 当前位于 `com.abilitykit.ability`。这些类型直接表达 Ability 组合与项目目录语义，因此在纯 C# authoring model 下沉或建立不依赖 Ability 的窄接口之前，完整 Workspace 不迁入 `com.abilitykit.triggering/Editor`。

该边界避免形成 `triggering editor -> ability editor/runtime -> triggering editor` 循环依赖。`com.abilitykit.base.editor` 的 Editor Platform 只提供领域无关基础设施，也不会引用 Ability 或 Triggering。

### 9.2 Workspace 分层

`TriggerAuthoringWorkspaceWindow` 负责生命周期、命令注册、选择、刷新和操作编排，具体 UI 拆分为：

- `TriggerAuthoringProjectTreePanel`：项目、模块和未分配模块导航；
- `TriggerAuthoringModuleContentPanel`：承载 Ability-owned module drawer；
- `TriggerAuthoringSourceSyncPanel`：显示规范化同步状态并发起 import/export；
- `TriggerAuthoringProjectValidationPanel`：显示结构化项目诊断并执行 Validate/Export Runtime。

模块 drawer 实现 `IDisposable`；Inspector/Workspace 关闭时必须释放 drawer、command registrations、Localization/Editor 回调和 repaint subscription。窗口只是编排层，不能重新吸收 validator、codec 或 exporter 的业务实现。

### 9.3 Editor Platform 接入

Trigger Editor 已渐进采用：

- 稳定 command id 与本地化 label/tooltip；
- Source Sync Card 及 InSync、LocalChanged、SourceChanged、Conflict、Untracked、SourceMissing、InvalidSource 状态策略；
- Platform Diagnostics adapter，保留 code、severity、path、target 和 locate action；
- module/template Source JSON 与 runtime plan export 的 `EditorAtomicFileWriter`；
- UTF-8 no BOM、内容不变跳过、replace fallback，以及 `.abilitykit.tmp.*` / `.abilitykit.bak.*` 清理语义。

项目诊断定位按 validator 的真实 path 映射：`project.modules[i]` 定位 module asset，`project.templates[i]` 定位 template asset，event/global-blackboard/template catalog 路径定位对应 catalog，其余路径回退到 project asset。Platform 不解析这些 Trigger path。

### 9.4 同步与导出安全边界

Source Sync 使用资产和 source 的规范化 hash 与 last-synchronized baseline 判断状态。双方同时修改时进入 Conflict；只有显式 force 才允许覆盖冲突侧。Import 必须在变更前记录 Unity Undo，并在成功后更新 baseline、标记 dirty；Export 更新 source 和 baseline，但不能在校验失败时破坏旧产物。

runtime plan 是从 authoring 编译出的可重建产物，不是 authoring 权威源。Project export 在写盘前运行领域 validator；atomic writer 只保证文件替换安全，不替代 schema 校验或 identity 检查。

### 9.5 测试证据与限制

现有 Editor 测试源码覆盖 golden authoring validation、runtime plan 构建/可加载性、Source Sync 状态、force import/export 冲突收敛、临时/备份文件清理、dirty、Undo/Redo 和 diagnostic adapter/location。相关 Ability Editor Tests 项目图已通过定向 `dotnet msbuild` 编译，0 errors。

本轮未运行 Unity Test Runner。上述新增 NUnit/EditMode 测试只能声明“源码已编译”，不能声明实际通过；尤其 Unity Undo/Redo、AssetDatabase refresh、Selection/Ping 定位和 Domain Reload 必须在 Unity 侧执行验收。

### 9.6 后续下沉前置条件

将通用 Trigger authoring UI 下沉至 Triggering Editor 前，至少需要：

1. 把不依赖 Ability 的 authoring DTO/schema、identity 和 validation contract 下沉到纯 C# 层，或提供窄接口；
2. 明确 project/module/template/catalog 中哪些是 Triggering 通用概念，哪些仍是 Ability 聚合；
3. 保持 source/runtime round-trip、迁移版本和诊断路径稳定；
4. 为迁移后的 asmdef 依赖无环、资产兼容和 golden export 建立门禁。

---

## 十、设计总结

### 10.1 核心优势

| 触发器类型 | 优势 |
|------------|------|
| **通用触发器** | 完全解耦、配置驱动、热更新支持 |
| **强类型触发器** | 类型安全、性能好、IDE 支持 |
| **直接调用** | 零开销、最简单 |

### 10.2 选择决策树

```
开始
  │
  ├─ 需要运行时配置吗？
  │    │
  │    ├─ 是 ──▶ 通用触发器
  │    │
  │    └─ 否 ──┬─ 订阅方和发布方是否必须解耦？
  │              │
  │              ├─ 是 ──▶ 强类型触发器
  │              │
  │              └─ 否 ──▶ 直接方法调用
  │
  └─ 性能是否极度敏感？
       │
       ├─ 是 ──▶ 直接方法调用
       │
       └─ 否 ──▶ 继续上面的决策
```

### 10.3 文件清单

| 文件路径 | 说明 |
|----------|------|
| **核心接口** | |
| `Runtime/Ability/Triggering/Interface/IEventBus.cs` | 事件总线接口 |
| `Runtime/Ability/Triggering/Runtime/ITriggerActionRunner.cs` | 动作运行器接口 |
| `Runtime/Ability/Triggering/Runtime/IRunningAction.cs` | 运行动作接口 |
| `Runtime/Ability/Triggering/Interface/ITriggerContextFactory.cs` | 上下文工厂接口 |
| **核心实现** | |
| `Runtime/Ability/Triggering/CommonEventDispatcherBus.cs` | 通用事件总线实现 |
| `Runtime/Ability/Triggering/TriggerRegistry.cs` | 触发器注册表 |
| `Runtime/Ability/Triggering/Runtime/TriggerActionRunner.cs` | 动作运行器实现 |
| **条件和动作** | |
| `Runtime/Ability/Triggering/Builtins/AnyCondition.cs` | Any 条件 |
| `Runtime/Ability/Triggering/Builtins/SetNumericVarAction.cs` | 设置变量动作 |
| `Runtime/Ability/Triggering/Builtins/NumericVarGreaterThanCondition.cs` | 数值比较条件 |
| **变量系统** | |
| `Runtime/Ability/Triggering/Variables/Numeric/TriggerContextNumericVarExtensions.cs` | 变量扩展方法 |
| `Runtime/Ability/Triggering/Variables/Numeric/Domains/LocalNumericVarDomain.cs` | 局部变量域 |
| `Runtime/Ability/Triggering/Variables/Numeric/Domains/GlobalNumericVarDomain.cs` | 全局变量域 |
| **强类型实现** | |
| `Runtime/Impl/Moba/Services/Buffs/MobaBuffTriggering.cs` | Buff 强类型触发器 |
| `Runtime/Impl/Moba/Services/Skill/Events/MobaSkillTriggering.cs` | 技能强类型触发器 |
| **Authoring Editor（Ability-owned）** | |
| `Editor/Windows/TriggerAuthoringWorkspaceWindow.cs` | Workspace 生命周期、命令与操作编排 |
| `Editor/Windows/TriggerAuthoringWorkspacePanels.cs` | Tree/Content/Source Sync/Validation panels |
| `Editor/Utilities/TriggerAuthoringEditorIntegration.cs` | Platform Localization、Commands 与 Diagnostics adapter |
| `Editor/Utilities/TriggerAuthoringSourceSync.cs` | Module source sync、Undo、dirty 与 baseline |
| `Editor/Utilities/TriggerSourceCodec.cs` | Authoring Source JSON codec 与 atomic write 接入 |
| `Editor/Utilities/TriggerAuthoringRuntimeExporter.cs` | Runtime plan 编译、序列化与 atomic write 接入 |
| `Tests/Editor/TriggerAuthoringSourceSyncTests.cs` | 同步冲突、force、临时文件和 Undo/Redo 回归源码 |
