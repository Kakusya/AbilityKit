<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>AbilityKit.Demo.Moba.BattleFlow.Runner</RootNamespace>
    <AssemblyName>AbilityKit.Demo.Moba.BattleFlow.Runner</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- 内核已抽到 AbilityKit.Demo.Moba.Acceptance.LiveSim；这里只剩 CLI 壳（序列化场景 + 调内核 + 写结果）。 -->
    <ProjectReference Include="..\AbilityKit.Scenario\AbilityKit.Scenario.csproj" />
    <ProjectReference Include="..\AbilityKit.Demo.Moba.Acceptance.LiveSim\AbilityKit.Demo.Moba.Acceptance.LiveSim.csproj" />
  </ItemGroup>
</Project>
